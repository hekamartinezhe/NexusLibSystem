<Window x:Class="NexusLibrarySystem.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        >
    <Grid Background="{StaticResource BackgroundBrush}">
        <Border Width="350" Padding="30"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                >
            <StackPanel>
                <TextBlock Text="Welcome" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Please log in to your account" FontSize="14" Margin="0,0,0,20"/>

                <Label Content="Enrollment Number"/>
                <TextBox x:Name="TxtEnrollment" Style="{StaticResource RoundedTextBox}"/>

                <Label Content="Password"/>
                <PasswordBox x:Name="TxtPassword" Style="{StaticResource RoundedPasswordBox}"/>

                <Button Content="Login"
                        Click="Login_Click"
                        Style="{StaticResource MaterialButton}"
                        Height="35"/>
                <TextBlock x:Name="LblError" Foreground="Red" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

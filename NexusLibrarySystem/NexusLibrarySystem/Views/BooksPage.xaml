<Page x:Class="NexusLibrarySystem.Views.BooksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:NexusLibrarySystem.Views"
      Title="Books">

    <Grid Background="{StaticResource BackgroundBrush}" Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Book Management"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="{StaticResource OnBackgroundBrush}"
                   Margin="0,0,0,20"/>

        <!-- Search Bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBox x:Name="SearchBox"
                     Width="300"
                     Height="35"
                     VerticalContentAlignment="Center"
                     Style="{StaticResource RoundedTextBox}">
                <TextBox.ToolTip>
                    <ToolTip Content="Search by title..." />
                </TextBox.ToolTip>
            </TextBox>

            <Button Content="Search"
                    Width="100"
                    Height="35"
                    Margin="10,0,0,0"
                    Click="Search_Click"
                    Style="{StaticResource MaterialButton}"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button x:Name="LoanButton" Content="Loan Book" Width="120" Height="35" Margin="0,0,10,0"
                    Click="LoanButton_Click" Visibility="Collapsed" Style="{StaticResource MaterialButton}"/>
            <Button x:Name="AddBookButton" Content="Add" Width="90" Height="35" Margin="0,0,10,0"
                    Click="AddBookButton_Click" Visibility="Collapsed" Style="{StaticResource MaterialButton}"/>
            <Button x:Name="EditBookButton" Content="Edit" Width="90" Height="35" Margin="0,0,10,0"
                    Click="EditBookButton_Click" Visibility="Collapsed" Style="{StaticResource MaterialButton}"/>
            <Button x:Name="DeleteBookButton" Content="Delete" Width="90" Height="35"
                    Click="DeleteBookButton_Click" Visibility="Collapsed" Style="{StaticResource MaterialButton}"/>
        </StackPanel>

        <!-- Book Table -->
        <Border Grid.Row="3"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="8"
                Padding="5"
                BorderBrush="{StaticResource PrimaryBrush}"
                BorderThickness="1">

            <DataGrid x:Name="BooksGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      RowBackground="White"
                      AlternatingRowBackground="#F5F5F5"
                      GridLinesVisibility="Horizontal"
                      Background="Transparent"
                      BorderThickness="0"
                      FontSize="14"
                      RowHeight="35"
                      ColumnHeaderHeight="40"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource OnPrimaryBrush}"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,5"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                    <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
